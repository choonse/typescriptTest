//일등함수
let fTest = function(a:number,b:number):number {return a+b}
fTest =  function(a,b) {return a-b}


//함수 호출

let functionExpression = function(a:number,b:number):number {return a+b}
let value = functionExpression(1,2);
